## trendminer_interface.objects.fingerprint.\_fingerprint.FingerprintDefinition

Definition for a fingerprint

Attributes:

| Name      | Type                     | Description                                 |
| --------- | ------------------------ | ------------------------------------------- |
| `entries` | \`list\[Tag              | Attribute\]\`                               |
| `layers`  | `list[FingerprintLayer]` | List of layers included in the fingerprint. |

### entries

```
entries: list[Attribute | Tag] = entries
```

### layers

```
layers: list[FingerprintLayer] = layers
```

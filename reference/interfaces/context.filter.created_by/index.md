# `client.context.filter.created_by`

## trendminer_interface.\_client.context.filter.created_by_filter.ContextCreatedByFilterFacade

Facade for creating context created-by filters

### new

```
new(users: list[User]) -> ContextCreatedByFilter
```

Filter on context item creator

Parameters:

| Name    | Type         | Description              | Default    |
| ------- | ------------ | ------------------------ | ---------- |
| `users` | `list[User]` | Users on which to filter | *required* |

Returns:

| Type                     | Description |
| ------------------------ | ----------- |
| `ContextCreatedByFilter` |             |
